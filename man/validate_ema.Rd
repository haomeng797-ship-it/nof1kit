% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_ema.R
\name{validate_ema}
\alias{validate_ema}
\title{Check EMA records for the problems that actually occur}
\usage{
validate_ema(data, ranges = NULL, required = "timestamp", n_days = NULL)
}
\arguments{
\item{data}{A data frame from \code{\link[=read_ema]{read_ema()}}.}

\item{ranges}{Named list of \code{c(min, max)} giving the valid range of each
measured variable, e.g. \code{list(mood = c(0, 100))}. Columns not listed are
not range-checked.}

\item{required}{Character vector of columns that must not be missing.}

\item{n_days}{Optional study length. When given, records with a \code{study_day}
outside \code{1:n_days} are flagged.}
}
\value{
An object of class \code{nof1_validation}: a list with \code{n_records},
\code{n_issues}, and a data frame \code{issues} with columns \code{check}, \code{row},
\code{column}, and \code{detail}. \code{n_issues == 0} means every check passed.
}
\description{
Runs the integrity checks a single-case dataset needs before analysis:
values outside their declared range, duplicated timestamps, missing values in
required columns, records falling outside the study period, and timestamps
that failed to parse.
}
\details{
The result is returned as data, not printed. Reports are one way to consume
it; a preregistered pipeline that halts on failure is another.
}
\examples{
d <- data.frame(
  timestamp = as.POSIXct(c("2026-02-18 09:00:00", "2026-02-18 09:00:00"), tz = "UTC"),
  mood = c(72, 140),
  study_day = c(1L, 1L)
)
v <- validate_ema(d, ranges = list(mood = c(0, 100)))
v

}
