% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_ema.R
\name{read_ema}
\alias{read_ema}
\title{Read EMA records into a tidy data frame}
\usage{
read_ema(path, start_date = NULL, tz = "UTC", timestamp_col = "timestamp")
}
\arguments{
\item{path}{Path to a \code{.csv} or \code{.json} file.}

\item{start_date}{Date (or something \code{\link[=as.Date]{as.Date()}} accepts) on which the study
began. Used to compute \code{study_day} when the file does not already have it.}

\item{tz}{Time zone to interpret timestamps in. Defaults to \code{"UTC"}, which
matches the ISO 8601 output of the companion app.}

\item{timestamp_col}{Name of the column holding the timestamp. Real files
call it many things (\code{datetime}, \code{time}, \code{recorded_at}); give the name here
and it is renamed to \code{timestamp} on the way in.}
}
\value{
A data frame with \code{timestamp} as \code{POSIXct}, \code{study_day} as integer
when derivable, and all other columns unchanged.
}
\description{
Reads the CSV or JSON produced by a mobile collection tool and returns a
data frame with parsed timestamps and a study-day index, ready for
\code{\link[=validate_ema]{validate_ema()}} and \code{\link[=compliance]{compliance()}}.
}
\details{
The expected columns are \code{timestamp} plus whatever was measured. Only
\code{timestamp} is required. If \code{start_date} is supplied (or the file already
carries a \code{study_day} column) a \code{study_day} index is attached, counting from
1 on the first day of the study.
}
\examples{
f <- system.file("extdata", "example_ema.csv", package = "nof1kit")
if (nzchar(f)) head(read_ema(f))

}
